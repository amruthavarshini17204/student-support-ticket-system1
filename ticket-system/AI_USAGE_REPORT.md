# Mandatory AI Usage Report

**AI TOOL USED:** Claude (Anthropic)

**WHAT I ASKED AI TO DO:**
1. Scaffold a full Spring Boot backend (entities, repositories, service layer,
   REST controllers, exception handling) for a Student Support & Ticket
   Management system, given the assignment brief
2. Design the SLA/ageing/auto-escalation logic (SLA windows per priority, breach
   detection, escalation-before-breach) and the valid ticket status-transition rules
3. Build a plain HTML/CSS/JS frontend that talks to the REST API (ticket list with
   filters, create-ticket form, ticket detail with activity timeline, manager
   dashboard) and write the approach note / README

**PROMPT THAT WAS MOST USEFUL:**
"Build a working prototype for [assignment brief] — decide users, workflows,
permissions, reports, data model, architecture yourself" — letting the AI make and
justify the product decisions (SLA policy, status machine, escalation rule) rather
than asking for boilerplate CRUD produced a more defensible design.

**CODE GENERATED BY AI:** All of it — entities, repositories, `TicketService`,
`SlaService`, controllers, DTOs, exception handling, the seed data, the frontend
(`index.html` / `style.css` / `app.js`), and this README.

**CODE I MODIFIED:** _[Fill in after you run it — e.g. any UI tweaks, wording
changes, or logic you adjusted after testing.]_

**AI OUTPUT THAT WAS WRONG:** _[Fill in from your own run — I generated this
without being able to run a live `mvn spring-boot:run` in my sandbox (no access to
Maven Central), so I reviewed the code manually for correctness rather than
verifying it end-to-end. Run it yourself, click through create → assign → status
update → comment → dashboard, and note anything that breaks or behaves oddly.]_

**HOW I IDENTIFIED THE PROBLEM:** _[e.g. ran the app, tested a specific flow, got an
error / wrong result]_

**HOW I FIXED IT:** _[what you changed and why]_

---
*Note: this report is a starting draft. For genuine credibility with the reviewers,
run the app yourself before submitting and replace the bracketed sections above with
what you actually found — that's the point of the exercise.*
